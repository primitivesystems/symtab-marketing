import type { DownloadUrls } from "@workspace/ui/lib/platform"

export const announcement = {
  text: "We’re building Symtab in the open. Join our community.",
  href: "/#community",
}

export const downloads: DownloadUrls = {
  macos: "/download",
  windows: "/download",
  linux: "/download",
  fallback: "/download",
}
